name: test

channels:
  - conda-forge

dependencies:
  # Base depends
  - python
  - pip

  - bibtexparser
  - numpy
  - seamm-util
  - tabulate
  
  # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov

  # Documentation
  - pydata-sphinx-theme
  - sphinx-design

  # Pip-only installs
  - pip:
      - ase
      # Documentation
      - sphinx-copybutton

