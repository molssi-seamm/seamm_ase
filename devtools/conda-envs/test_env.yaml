name: test
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov

    # Pip-only installs
  - pip:
      - ase
      # Documentation
      - sphinx-copybutton
      - sphinx-design

